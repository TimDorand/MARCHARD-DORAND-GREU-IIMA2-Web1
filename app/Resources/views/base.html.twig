<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('bundles/app/css/home/home.css') }}" type="text/css" media="all" />
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}
            <header class="header">
                <a href="{{ path('article_list') }}" class="logo"> Mon blog </a>
                <a href="{{ path('article_new') }}" class="new-button"> New article </a>
            </header>

        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
